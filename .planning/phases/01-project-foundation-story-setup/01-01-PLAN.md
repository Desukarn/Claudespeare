---
phase: 01-project-foundation-story-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - stories/PROJECT_TEMPLATE.md
  - stories/.gitkeep
  - .planning/templates/story-project.md
autonomous: true
requirements:
  - SETUP-01
  - SETUP-02
  - SETUP-03
  - SETUP-04

must_haves:
  truths:
    - Writer can create new story project with title
    - Writer can define genre for the story
    - Writer can write premise/logline
    - Writer can define core themes
    - All project metadata is stored and retrievable
  artifacts:
    - path: "stories/PROJECT_TEMPLATE.md"
      provides: "Template for new story projects"
      min_lines: 40
    - path: ".planning/templates/story-project.md"
      provides: "Story project initialization template"
      min_lines: 30
  key_links:
    - from: ".planning/templates/story-project.md"
      to: "stories/{project-name}/PROJECT.md"
      via: "instantiation during project creation"
      pattern: "cp.*story-project\\.md"
---

<objective>
Create the foundational structure for initializing new story projects with essential metadata (title, genre, premise, themes).

Purpose: Establishes the basic project structure that all Claudespeare stories will use, providing writers with a standardized template for capturing core story information from the start.

Output: Template files and directory structure for story project initialization.
</objective>

<execution_context>
@/home/node/.claude/get-shit-done/workflows/execute-plan.md
@/home/node/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create stories directory structure</name>
  <files>stories/.gitkeep, stories/PROJECT_TEMPLATE.md</files>
  <action>
    Create the stories/ directory at project root with:
    1. .gitkeep file to ensure directory is tracked by git
    2. PROJECT_TEMPLATE.md as reference documentation showing the structure of a story project

    PROJECT_TEMPLATE.md should document:
    - Directory structure: stories/{story-name}/PROJECT.md, CHARACTERS.md, OUTLINE.md, etc.
    - File naming conventions (lowercase-with-hyphens for story names)
    - Purpose of each standard file
    - Example showing a fantasy story setup

    This is documentation, not a template that gets copied. Minimum 40 lines with clear sections.
  </action>
  <verify>
    <automated>test -d stories && test -f stories/.gitkeep && test -f stories/PROJECT_TEMPLATE.md && wc -l stories/PROJECT_TEMPLATE.md | awk '{if ($1 >= 40) exit 0; else exit 1}'</automated>
  </verify>
  <done>stories/ directory exists with .gitkeep and PROJECT_TEMPLATE.md documentation (40+ lines)</done>
</task>

<task type="auto">
  <name>Task 2: Create story project initialization template</name>
  <files>.planning/templates/story-project.md</files>
  <action>
    Create .planning/templates/story-project.md as the actual template file that will be copied when writers create new story projects.

    Template structure:
    ```markdown
    # {STORY_TITLE}

    ## Metadata

    **Title:** {STORY_TITLE}
    **Genre:** {GENRE}
    **Status:** Planning
    **Created:** {DATE}

    ## Premise

    {1-2 sentence logline describing the core story concept}

    ## Core Themes

    - {Theme 1}
    - {Theme 2}
    - {Theme 3}

    ## Story Elements

    - Characters: See CHARACTERS.md
    - Plot Outline: See OUTLINE.md
    - Character Arcs: See ARCS.md
    - World Building: See WORLD.md

    ## Notes

    {Space for writer's development notes}
    ```

    Use placeholder syntax {VARIABLE} that can be replaced during project creation. Keep it simple and writer-focused. Minimum 30 lines.
  </action>
  <verify>
    <automated>test -f .planning/templates/story-project.md && grep -q "{STORY_TITLE}" .planning/templates/story-project.md && grep -q "{GENRE}" .planning/templates/story-project.md && wc -l .planning/templates/story-project.md | awk '{if ($1 >= 30) exit 0; else exit 1}'</automated>
  </verify>
  <done>.planning/templates/story-project.md exists with placeholder variables and minimum 30 lines</done>
</task>

</tasks>

<verification>
1. stories/ directory structure is in place with documentation
2. Template file exists with proper placeholder syntax
3. All files committed to git
</verification>

<success_criteria>
- Writer has clear documentation on story project structure (PROJECT_TEMPLATE.md)
- Template file ready for instantiation when creating new stories (story-project.md)
- Foundation in place for SETUP-01 through SETUP-04 (title, genre, premise, themes)
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation-story-setup/01-01-SUMMARY.md`
</output>
