---
phase: 01-project-foundation-story-setup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/templates/characters.md
  - .planning/templates/outline.md
autonomous: true
requirements:
  - STORY-01
  - STORY-02

must_haves:
  truths:
    - Writer can create character profiles with name, role, and personality traits
    - Writer can outline plot structure with major beats
    - Character and plot templates are ready for use
  artifacts:
    - path: ".planning/templates/characters.md"
      provides: "Template for character profiles"
      min_lines: 40
    - path: ".planning/templates/outline.md"
      provides: "Template for plot structure"
      min_lines: 35
  key_links:
    - from: ".planning/templates/characters.md"
      to: "stories/{project-name}/CHARACTERS.md"
      via: "instantiation during project creation"
      pattern: "character profile structure"
    - from: ".planning/templates/outline.md"
      to: "stories/{project-name}/OUTLINE.md"
      via: "instantiation during project creation"
      pattern: "plot beat structure"
---

<objective>
Create templates for character development and plot outlining, enabling writers to define their story's cast and narrative structure.

Purpose: Provides structured formats for capturing character details and plot progression, ensuring writers have comprehensive tools for story planning before drafting begins.

Output: Character profile and plot outline template files.
</objective>

<execution_context>
@/home/node/.claude/get-shit-done/workflows/execute-plan.md
@/home/node/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create character profile template</name>
  <files>.planning/templates/characters.md</files>
  <action>
    Create .planning/templates/characters.md with structured sections for character development:

    ```markdown
    # Characters: {STORY_TITLE}

    ## Main Characters

    ### {CHARACTER_NAME}

    **Role:** {Protagonist/Antagonist/Supporting}
    **Age:** {Age}
    **Occupation:** {Occupation}

    **Personality Traits:**
    - {Trait 1}
    - {Trait 2}
    - {Trait 3}

    **Background:**
    {Brief background - upbringing, key life events, formative experiences}

    **Goals:**
    - External: {What they want in the story}
    - Internal: {What they need to learn/overcome}

    **Conflicts:**
    {What stands in their way}

    **Voice/Speech Patterns:**
    {How they speak, distinctive phrases, tone}

    ---

    ## Supporting Characters

    ### {CHARACTER_NAME}

    **Role:** {Role in story}
    **Relationship to Main Character:** {Relationship}

    **Key Traits:**
    - {Trait 1}
    - {Trait 2}

    **Purpose in Story:**
    {Why this character exists}

    ---

    ## Character Relationships

    {Map connections between characters}
    ```

    Provide sections for multiple characters with clear structure. Use {PLACEHOLDER} syntax. Minimum 40 lines.
  </action>
  <verify>
    <automated>test -f .planning/templates/characters.md && grep -q "{CHARACTER_NAME}" .planning/templates/characters.md && grep -q "Personality Traits:" .planning/templates/characters.md && wc -l .planning/templates/characters.md | awk '{if ($1 >= 40) exit 0; else exit 1}'</automated>
  </verify>
  <done>.planning/templates/characters.md exists with character profile structure (40+ lines)</done>
</task>

<task type="auto">
  <name>Task 2: Create plot outline template</name>
  <files>.planning/templates/outline.md</files>
  <action>
    Create .planning/templates/outline.md with classic story structure framework:

    ```markdown
    # Plot Outline: {STORY_TITLE}

    ## Story Structure

    ### Act 1: Setup

    **Inciting Incident:**
    {Event that disrupts the protagonist's normal world}

    **First Plot Point:**
    {Point of no return - protagonist commits to journey}

    **Key Scenes:**
    1. {Scene description}
    2. {Scene description}
    3. {Scene description}

    ### Act 2: Confrontation

    **Midpoint:**
    {Major revelation or shift in story direction}

    **Complications:**
    - {Complication 1}
    - {Complication 2}
    - {Complication 3}

    **Second Plot Point:**
    {Darkest moment, all seems lost}

    **Key Scenes:**
    1. {Scene description}
    2. {Scene description}
    3. {Scene description}

    ### Act 3: Resolution

    **Climax:**
    {Final confrontation, highest tension}

    **Resolution:**
    {How conflicts are resolved}

    **Key Scenes:**
    1. {Scene description}
    2. {Scene description}

    ## Subplots

    ### {Subplot Name}
    {Brief description and how it weaves through main plot}

    ## Plot Threads to Track

    - [ ] {Thread 1}
    - [ ] {Thread 2}
    - [ ] {Thread 3}
    ```

    Use three-act structure with clear beat markers. Use {PLACEHOLDER} syntax. Minimum 35 lines.
  </action>
  <verify>
    <automated>test -f .planning/templates/outline.md && grep -q "Act 1: Setup" .planning/templates/outline.md && grep -q "Act 2: Confrontation" .planning/templates/outline.md && grep -q "Act 3: Resolution" .planning/templates/outline.md && wc -l .planning/templates/outline.md | awk '{if ($1 >= 35) exit 0; else exit 1}'</automated>
  </verify>
  <done>.planning/templates/outline.md exists with three-act structure and plot beats (35+ lines)</done>
</task>

</tasks>

<verification>
1. Character template provides comprehensive profile structure
2. Plot outline template uses standard three-act framework
3. Both templates use placeholder syntax for easy instantiation
4. All files committed to git
</verification>

<success_criteria>
- Writers can define detailed character profiles with personality, goals, conflicts (STORY-01)
- Writers can outline plot structure with major story beats (STORY-02)
- Templates ready for instantiation when creating new story projects
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation-story-setup/01-02-SUMMARY.md`
</output>
