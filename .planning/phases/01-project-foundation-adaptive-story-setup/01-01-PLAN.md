---
phase: 01-project-foundation-adaptive-story-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/write/init-project.md
  - .claude/skills/write/SKILL.md
autonomous: true
requirements:
  - INIT-01
  - INIT-02
  - INIT-03
  - INIT-04
  - INIT-05

must_haves:
  truths:
    - Writer can execute command to start new story project
    - Writer can select story length (short story, novella, novel)
    - Writer can select workflow mode (YOLO vs In-Depth)
    - Writer provides title, genre, and premise
    - System creates project directory with base files
  artifacts:
    - path: ".claude/skills/write/init-project.md"
      provides: "Project initialization workflow"
      min_lines: 100
    - path: ".claude/skills/write/SKILL.md"
      provides: "Skill index with init command"
      exports: ["write:init"]
  key_links:
    - from: "Skill command"
      to: "init-project.md workflow"
      via: "Command execution"
      pattern: "write:init"
---

<objective>
Create the foundation for story project initialization, enabling writers to start new projects with configurable depth and mode.

Purpose: Establish the entry point for all story projects - a structured initialization that captures essential information (title, genre, premise) and writer preferences (story length, workflow mode) before routing to appropriate templates.

Output: Working initialization command that creates project directory structure and captures foundational story information.
</objective>

<execution_context>
@/home/node/.claude/get-shit-done/workflows/execute-plan.md
@/home/node/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

Existing templates to reference:
@.planning/templates/story-project.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skill directory structure and index</name>
  <files>.claude/skills/write/SKILL.md</files>
  <action>
Create the write skill directory structure and SKILL.md index file. This establishes the command namespace for story writing.

Directory: `.claude/skills/write/`

SKILL.md contents:
- Title: "Write - Story Development Skill"
- Description: Commands for creative writing workflow
- Commands list:
  - `/write:init` - Initialize new story project (links to init-project.md)
  - (Placeholder comments for future commands like `/write:chapter`, `/write:revise`)
- Format follows GSD skill pattern (see /home/node/.claude/get-shit-done/.claude/skills/gsd/SKILL.md for reference structure)

Keep SKILL.md lightweight (~50 lines) - it's an index, not implementation.
  </action>
  <verify>
    <automated>test -f .claude/skills/write/SKILL.md && grep -q "write:init" .claude/skills/write/SKILL.md</automated>
  </verify>
  <done>SKILL.md exists, lists write:init command, follows skill index pattern</done>
</task>

<task type="auto">
  <name>Task 2: Create initialization workflow</name>
  <files>.claude/skills/write/init-project.md</files>
  <action>
Create the project initialization workflow that guides writers through setup questions and creates the project structure.

Workflow structure:

1. **Welcome section**: Explain what initialization does
2. **Questions to ask**:
   - Story title (required)
   - Story length: short story (<7500 words), novella (7500-40000), novel (40000+)
   - Workflow mode: YOLO (quick start) or In-Depth (detailed planning)
   - Genre (fantasy, sci-fi, romance, mystery, literary, other)
   - Premise/logline (1-2 sentences describing core story concept)

3. **Validation**: Title is unique (check if stories/{slug}/ exists)

4. **Directory creation**:
   - Create `stories/{title-slug}/`
   - Create `stories/{title-slug}/PROJECT.md` from template (populate with provided info)
   - Add metadata section recording: length, mode, genre, creation date

5. **Next steps routing**:
   - If YOLO mode → Explain that `/write:expand` will be created in Phase 1 Plan 02 (placeholder for now)
   - If In-Depth mode → Explain that detailed planning workflows will be created in Phase 1 Plan 03 (placeholder for now)
   - For v1: Just create PROJECT.md and inform user next steps are coming

6. **Output**: Confirm project created, show PROJECT.md path, explain what happens next based on mode

Implementation notes:
- Use gsd-tools.cjs as reference for file operations (if needed)
- Store length and mode in PROJECT.md frontmatter for later phases to read
- Title slug: lowercase, hyphens for spaces, alphanumeric only
- Frontmatter format:
  ```yaml
  ---
  title: "Story Title"
  genre: fantasy
  length: novel
  mode: yolo
  status: planning
  created: 2026-02-26
  ---
  ```
  </action>
  <verify>
    <automated>test -f .claude/skills/write/init-project.md && grep -q "story length" .claude/skills/write/init-project.md</automated>
  </verify>
  <done>
Workflow file exists with:
- Clear question sequence for title, length, mode, genre, premise
- Logic for creating stories/{slug}/ directory
- PROJECT.md generation with frontmatter
- Next-step routing based on mode selection
  </done>
</task>

</tasks>

<verification>
After completing both tasks:

1. Verify skill structure: `.claude/skills/write/SKILL.md` exists and lists `/write:init`
2. Verify workflow exists: `.claude/skills/write/init-project.md` exists with complete initialization flow
3. Verify question coverage: Workflow asks for all INIT requirements (title, genre, premise, length, mode)
4. Verify routing logic: Workflow explains different next steps for YOLO vs In-Depth mode
</verification>

<success_criteria>
- Command `/write:init` is discoverable via SKILL.md index
- Workflow captures all required initialization data (INIT-01 through INIT-05)
- Project directory structure is created with proper frontmatter
- Writers understand what happens next based on their mode selection
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation-adaptive-story-setup/01-01-SUMMARY.md`
</output>
