---
description: Compare two chapter versions showing word count changes and paragraph-level diffs
---

Compare two versions of a chapter to see what changed.

Read and follow the `/write:diff` section in `.claude/skills/write/chapter-versions.md`.

Arguments: $ARGUMENTS

Expected format: `<chapter-name> <version1> <version2>` (e.g., `chapter-1 1 3` or `1 v02 current`)

If arguments incomplete, ask the user for chapter name and the two versions to compare.
